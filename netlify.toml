[build]
  command   = "npm install"   # o pon "npm run build" si usas bundler
  publish   = "."             # tu carpeta pública
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"
  node_version = "18.19.0"
