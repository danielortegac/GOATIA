[build]
  command = "npm install"
  publish = "."
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"
  timeout = 28

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
